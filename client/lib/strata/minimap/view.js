exports.drawMinimap = function () {
  throw new Error('not implemented')

  // minimap = document.querySelector('#minimap')
  //
  // let minimapImageId = strata[path].div.id + "_minimap_img"
  // let minimapImageDivId = minimapImageId + "_div"
  //
  // if (!minimap.querySelector(minimapImageDivId)) {
  //     strata[path].imageSrc = strata[path].div.querySelector('canvas').toDataURL()
  //
  //     let minimapImageDiv = document.createElement('div')
  //     minimapImageDiv.id = minimapImageDivId
  //     minimapImageDiv.style.backgroundColor = strata[path].bgColor
  //     minimap.appendChild(minimapImageDiv)
  //     minimapImageDiv = minimap.querySelector(#{minimapImageDivId})
  //
  //     let minimapImage = new Image()
  //     minimapImage.id = minimapImageId
  //     minimapImage.src = strata[path].imageSrc
  //     minimapImage.className = "minimap-img"
  //     minimapImageDiv.appendChild(minimapImage)
  // }
}
